using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//ok let me defend this bad decision:
//its 8:48pm on a monday and ive been fighting w2 for over 24 hours now
//because dams cause an infinite loop in the code and for some reason it's not 
public class HardcodedDam : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
